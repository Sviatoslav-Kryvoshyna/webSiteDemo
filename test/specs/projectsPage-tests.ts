import projectsPage from "../pageobjects/projects.page";

describe.skip('Projects page tests', () => {
    beforeEach(async () => {
        await projectsPage.open();
    });

    it('Should open Projects page', async () => {
    });
});